---
# Teardown logic for module_license_create
- name: Delete created resources
  thalesgroup.ciphertrust.license_create:
    op_type: delete
    localNode: "{{ localNode }}"
    name: "{{ generated_name | default(omit) }}"
    id: "{{ generated_id | default(omit) }}"
  ignore_errors: true
