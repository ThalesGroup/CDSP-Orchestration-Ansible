---
# Teardown logic for module_cm_resource_get_id_from_name
- name: Delete created resources
  thalesgroup.ciphertrust.cm_resource_get_id_from_name:
    op_type: delete
    localNode: "{{ localNode }}"
    name: "{{ generated_name | default(omit) }}"
    id: "{{ generated_id | default(omit) }}"
  ignore_errors: true
