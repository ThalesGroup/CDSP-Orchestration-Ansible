---
# Teardown logic for module_cte_client
- name: Delete created resources
  thalesgroup.ciphertrust.cte_client:
    op_type: delete
    localNode: "{{ localNode }}"
    name: "{{ generated_name | default(omit) }}"
    id: "{{ generated_id | default(omit) }}"
  ignore_errors: true
