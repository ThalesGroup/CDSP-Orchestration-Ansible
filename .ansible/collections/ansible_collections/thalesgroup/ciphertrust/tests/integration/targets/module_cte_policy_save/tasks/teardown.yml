---
# Teardown logic for module_cte_policy_save
- name: Delete created resources
  thalesgroup.ciphertrust.cte_policy_save:
    op_type: delete
    localNode: "{{ localNode }}"
    name: "{{ generated_name | default(omit) }}"
    id: "{{ generated_id | default(omit) }}"
  ignore_errors: true
