---
# Teardown logic for module_cte_client_group
- name: Delete created resources
  thalesgroup.ciphertrust.cte_client_group:
    op_type: delete
    localNode: "{{ localNode }}"
    name: "{{ generated_name | default(omit) }}"
    id: "{{ generated_id | default(omit) }}"
  ignore_errors: true
