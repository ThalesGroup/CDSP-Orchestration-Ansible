---
# Teardown logic for module_cm_certificate_authority
- name: Delete created resources
  thalesgroup.ciphertrust.cm_certificate_authority:
    op_type: delete
    localNode: "{{ localNode }}"
    name: "{{ generated_name | default(omit) }}"
    id: "{{ generated_id | default(omit) }}"
  ignore_errors: true
