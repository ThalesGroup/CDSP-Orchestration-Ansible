---
# Teardown logic for module_licensing_lockdata_get
- name: Delete created resources
  thalesgroup.ciphertrust.licensing_lockdata_get:
    op_type: delete
    localNode: "{{ localNode }}"
    name: "{{ generated_name | default(omit) }}"
    id: "{{ generated_id | default(omit) }}"
  ignore_errors: true
