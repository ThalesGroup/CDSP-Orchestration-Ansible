---
# Teardown logic for module_interface_save
- name: Delete created resources
  thalesgroup.ciphertrust.interface_save:
    op_type: delete
    localNode: "{{ localNode }}"
    name: "{{ generated_name | default(omit) }}"
    id: "{{ generated_id | default(omit) }}"
  ignore_errors: true
