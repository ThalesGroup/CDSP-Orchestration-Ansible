---
# Teardown logic for module_vault_keys2_op
- name: Delete created resources
  thalesgroup.ciphertrust.vault_keys2_op:
    op_type: delete
    localNode: "{{ localNode }}"
    name: "{{ generated_name | default(omit) }}"
    id: "{{ generated_id | default(omit) }}"
  ignore_errors: true
