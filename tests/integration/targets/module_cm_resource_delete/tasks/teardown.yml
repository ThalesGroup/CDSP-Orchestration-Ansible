---
# Teardown logic for module_cm_resource_delete
- name: Delete created resources
  thalesgroup.ciphertrust.cm_resource_delete:
    op_type: delete
    localNode: "{{ localNode }}"
    name: "{{ generated_name | default(omit) }}"
    id: "{{ generated_id | default(omit) }}"
  ignore_errors: true
