---
# Teardown logic for module_dpg_user_set_save
- name: Delete created resources
  thalesgroup.ciphertrust.dpg_user_set_save:
    op_type: delete
    localNode: "{{ localNode }}"
    name: "{{ generated_name | default(omit) }}"
    id: "{{ generated_id | default(omit) }}"
  ignore_errors: true
