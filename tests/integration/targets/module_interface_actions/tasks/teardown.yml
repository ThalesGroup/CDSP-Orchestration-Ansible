---
# Teardown logic for module_interface_actions
- name: Delete created resources
  thalesgroup.ciphertrust.interface_actions:
    op_type: delete
    localNode: "{{ localNode }}"
    name: "{{ generated_name | default(omit) }}"
    id: "{{ generated_id | default(omit) }}"
  ignore_errors: true
